const SUPABASE_URL="https://qodtxydbzqmymsuvamza.supabase.co";
const SUPABASE_KEY="sb_publishable_l9HUHlKsyhyYhY9JWuIncA_EOuDEKeD";
let produtos=[], categoriaAtual="Todos";
function dinheiro(v){return Number(v||0).toLocaleString("pt-BR",{minimumFractionDigits:2,maximumFractionDigits:2});}
async function carregarLoja(){const r=await fetch(`${SUPABASE_URL}/rest/v1/produtos?select=*&order=Id.desc`,{headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`}});produtos=await r.json();renderCategorias();renderLoja();}
function renderCategorias(){const cats=["Todos",...new Set(produtos.map(p=>p.Categoria).filter(Boolean))];categorias.innerHTML=cats.map(c=>`<button class="chip ${c===categoriaAtual?'active':''}" onclick="categoriaAtual='${c.replaceAll("'","\\'")}';renderCategorias();renderLoja()">${c}</button>`).join("");}
function renderLoja(){const q=(busca?.value||"").toLowerCase();const filtrados=produtos.filter(p=>(categoriaAtual==="Todos"||p.Categoria===categoriaAtual)&&(String(p.Nome||"").toLowerCase().includes(q)||String(p.Categoria||"").toLowerCase().includes(q)));produtosEl.innerHTML=filtrados.map(p=>{const imgs=(p.Imagem||"").split("|").map(x=>x.trim()).filter(Boolean),principal=imgs[0]||"",preco=Number(p.Preco||0),old=preco*1.18;return `<div class="card">${p.Destaque?'<div class="badge">🔥 DESTAQUE</div>':''}<div class="photo">${principal?`<img src="${principal}" onerror="this.style.display='none'">`:"🛒"}</div><div class="thumbs">${imgs.slice(0,5).map(i=>`<img src="${i}">`).join("")}</div><div class="info"><span class="cat">${p.Categoria||"Oferta"}</span><div class="title">${p.Nome||""}</div><div class="rating">★★★★★ 4.8</div><div class="old">R$ ${dinheiro(old)}</div><div class="price">R$ ${dinheiro(preco)} <span class="off">OFERTA</span></div><div class="free">Frete grátis em produtos selecionados</div><a class="buy" href="${p.Link||"#"}" target="_blank" rel="noopener noreferrer">Comprar no Mercado Livre</a></div></div>`}).join("")||"<div class='panel'>Nenhum produto encontrado.</div>";}
if(document.getElementById("produtosEl")) carregarLoja();